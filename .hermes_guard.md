# Hermes Guard: LinkThread Project Safety Rules

## 1. Absolute Path Integrity
- **Project Root:** `/Users/seonghoonjung/workspace/link-thread-project`
- **Rule:** Every file operation must start with this absolute path.
- **Danger:** Avoid sub-directories named `workspace` or `link-thread-project` that contain duplicate code.

## 2. Deployment Protocol
- **Command:** Always use `vercel --prod --yes --force` for production updates to bypass cache.
- **Mapping:** Immediately follow with `vercel alias set <deployment_url> voidsay.com` if the domain doesn't update automatically.

## 3. Verification Requirement
- **Action:** After every deployment, use the `browser_navigate` tool to visit `https://voidsay.com`.
- **Check:** Verify the specific UI change requested by the user is actually visible on the live site.
- **Cache-Busting:** Use a random query parameter like `https://voidsay.com/?t=123` to bypass edge caching during verification.

## 4. Cron Task Consistency
- Ensure the `hybrid_cron_task.py` always checks the live site status before proceeding with new feature work.
